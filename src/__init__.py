## the source would be considered as package itself and the __init__.py file would be used to initialize the package
# ## The __init__.py file can be empty or can contain package initialization code
# ## The __init__.py file can also be used to define the public API of the package by specifying 
        # which modules or functions should be accessible when the package is imported
## The __init__.py file can also be used to define the package version by setting the __version__ variable